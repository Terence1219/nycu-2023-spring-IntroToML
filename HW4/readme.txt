Required module: torch, torchvision, numpy, matplotlib, tqdm
Execution: python HW4.py
會先印出可使用的裝置(CPU or CUDA）
再來進行神經網路的訓練，共50個epochs，同時存取validation accuracy最高的model。
最後會存兩張圖，一張為accuracy，一張為loss。





